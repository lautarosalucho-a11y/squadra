import { GraphQLJSON } from 'graphql-scalars';

/**
 * Reexporta el scalar JSON para usarlo en los tipos GraphQL
 * (descripción rich text, valores de custom fields, payloads).
 */
export { GraphQLJSON };
